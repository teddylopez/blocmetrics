<h1> Show Page </h1>

<div class="row">
  	<div class="col-md-8">
  		<h4>URL: <%= link_to @registration.url, "http://#{@registration.url}" %></h4>
  		<p>Added by <%= @registration.user.email %></p>

 		<% @events.group_by(&:name).each do |name, events| %>
 			 <%= name %> | <%= events.count %>
 		<% end %>

 		<p><%= @events.count %> events exist for this app</p>
 		<p>Added by <%= @registration.user.email %></p>
  	</div>
</div>
