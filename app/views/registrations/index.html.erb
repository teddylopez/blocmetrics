  <%= link_to "Register an app", new_registration_path%>

  <% if current_user.registrations.any? %>
    <h1>Your registered apps:</h1>

  <table>
    <thead>
      <tr>
        <th>App</th>
        <th>App URL</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @registrations.each do |app| %>
        <tr>
          <td><%= link_to app.name, registration_path(app) %></td>
          <td><%= app.url %></td>

          <% if current_user == app.user %>
            <td><%= link_to 'Remove', registration_path(app), method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>

<% else %>
  <h1>You have not registered any apps</h1>
<%end %>
